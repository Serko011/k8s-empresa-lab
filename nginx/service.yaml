apiVersion: v1
kind: Service
metadata:
  name: nginx-gitops
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: nginx-gitops
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
